/**
 * Prints "hello, world" to the console.
 * @returns The greeting string that was printed.
 */
export const helloWorld = (): string => {
  const message = 'hello, world';
  console.log(message);
  return message;
};
