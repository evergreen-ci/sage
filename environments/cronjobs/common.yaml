# Common cronjob configuration shared across staging and prod
# This file is merged with environment-specific cronjob configs

cronjobs:
  - name: jira-polling
    schedule: '* * * * *' # Run every minute
    resources:
      requests:
        cpu: 500m
        memory: 1Gi
      limits:
        cpu: 1
        memory: 2Gi

