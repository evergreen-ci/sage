arch: "arm64"

ingress:
  enabled: true
  hosts:
    - parsley-ai-agent-skunkworks.skunkworks.staging.corp.mongodb.com
service:
  targetPort: 8080
mesh:
  enabled: true

envSecrets:
  MONGO_URL: parsley-ai-secrets
  MONGO_USERNAME: parsley-ai-secrets
  MONGO_PASSWORD: parsley-ai-secrets
  OPENAI_KEY: parsley-ai-secrets